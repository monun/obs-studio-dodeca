## 1. 검증 진입점과 기준 데이터

- [x] 1.1 `test/audio-tracks`에 이번 변경 전용 테스트 실행 파일과 opt-in CMake 연결을 추가한다. 선택한 플랫폼의 기존 preset으로 구성·빌드하고 테스트 목록 조회 및 실행이 실제로 되는지 확인한다.
- [x] 1.2 구버전 소스 데이터(0, 1·3·6번, 255, 배정 없음), 기존 프로필, 서로 다른 12개 시험음과 동기 표시 영상의 재현 fixture를 마련한다. 구 6트랙 빌드의 유효 배정과 기준 출력·동기 결과를 기록하고 fixture 생성 명령이 재실행 가능한지 확인한다.

## 2. 코어 용량과 소스 배정 이전

- [x] 2.1 `audio-io.h`와 `obs-output.h`의 믹스·출력 인코더 상한을 12로 맞추고 유효 비트 집합 및 상수 일치 검사를 추가한다. libobs와 관련 번들 모듈 빌드가 통과하고 12번째 믹스·출력 슬롯을 다루는 테스트가 통과하는지 확인한다.
- [x] 2.2 `audio-io.c`, `obs-audio.c`, `obs-source.c`, `obs-scene.c`, `obs-source-transition.c`, `obs-output.c`와 번들 오디오 렌더러의 배열·초기화·순회 경계를 맞춘다. 소스를 7·12번에 분리 배정한 시험과 중첩 장면·전환·음소거·필터·동기 오프셋 시험에서 기존 처리 의미와 신호 분리가 유지되는지 확인한다.
- [x] 2.3 새 소스 기본 배정을 `0xFFF`로 설정하고 배정 변경 API의 유효 범위를 정리한다. 생성·변경 신호·조회·저장 테스트에서 1~12번 상태가 일치하고 녹화 선택이나 기존 소스 배정이 바뀌지 않는지 확인한다.
- [x] 2.4 `obs.c`의 소스 저장·로드에 `dodeca_audio_tracks_version: 1` 표식과 구버전 6비트 이전을 구현한다. 255·0·배정 없음·오래된 모니터링 이전·알 수 없는 표식·신규 상위 트랙 저장/재로드·복제 fixture가 설계의 배정 표대로 동작하고 이전 처리가 반복되지 않는지 확인한다.

## 3. 설정과 UI

- [x] 3.1 `OBSBasic.cpp`의 트랙 기본값과 `OBSBasicSettings`의 프로필 읽기·쓰기를 `Track1`~`Track12`까지 확장한다. 기존 1~6번 이름·비트레이트·선택 보존, 신규 녹화 기본 1번, 7~12번 기본값 보정, 프로필 전환·재시작 복원이 통과하는지 확인한다.
- [x] 3.2 `OBSAdvAudioCtrl.*`를 트랙 체크박스 컬렉션으로 바꾸고 1~6/7~12 두 행을 구성한다. 12번 배정 변경이 실제 믹서에 반영되고 키보드 이동·접근성 이름으로 모든 트랙을 구분할 수 있는지 확인한다.
- [x] 3.3 `OBSBasicSettings.hpp/.cpp` 및 관련 Qt 폼의 간단·고급 일반 녹화, 사용자 지정 FFmpeg, 일반 FLV 트랙 선택을 컬렉션으로 처리한다. 12번 단독과 1·7·12번 선택의 저장·복원·선택 없음 경고를 확인하고 단일 트랙/리플레이 미지원 모드의 제약을 유지한다.
- [x] 3.4 고급 오디오 이름·비트레이트 편집 UI와 변경 신호·인코더 변경 시 보정·서비스 제어에 따른 활성 상태를 12개로 일반화한다. 10~12번에 서로 다른 값을 설정하고 코덱·출력 모드를 바꿔도 올바른 트랙에 적용되는지 확인한다.
- [x] 3.5 리플레이 및 방송 지연 용량 추정과 관련 경고를 트랙 컬렉션으로 계산한다. 선택한 1·7·12번 비트레이트 합과 간단 모드 공통 비트레이트 계산을 확인하고 12번 추가·해제 시 추정값이 함께 바뀌는지 확인한다.
- [x] 3.6 영어·한국어 트랙 표시와 제한 메시지 및 Qt 레이아웃을 정리한다. 기존 1~6번 번역 유지, 다른 로케일 fallback, 100%/200% 배율의 두 자리 번호·스크롤·탭·접근성 표시를 확인한다.

## 4. 녹화 인코더와 선택 연결

- [x] 4.1 `AdvancedOutput::GetAudioBitrate()`의 6개 문자열 배열을 번호 기반 키로 바꾸고, 고급 녹화·방송 및 간단 AAC/Opus 인코더 이름을 동적 문자열로 생성한다. 10~12번 설정 접근과 각 초기 이름의 고유성·잘림 없음이 확인되는 테스트를 통과시킨다.
- [x] 4.2 `AdvancedOutput`와 `SimpleOutput`의 녹화·리플레이 연결에서 선택 믹스를 오름차순 연속 슬롯에 배정하고 비활성 출력의 이전 연결을 정리한다. 1·7·12번의 세 스트림과 12개→12번 단독 재시작 출력의 정확한 트랙 수를 확인한다.
- [x] 4.3 녹화·방송·리플레이가 공유하는 인코더의 수명과 설정 적용을 검증하고 상위 트랙에 필요한 수정을 한다. 방송을 유지한 채 녹화·리플레이를 시작·중지·재시작해도 방송 믹스가 유지되고 녹화 트랙이 누락되지 않는지 확인한다.

## 5. 방송 선택과 번들 원격 제어

- [x] 5.1 고급 방송·VOD 선택 UI, `TrackIndex`/`VodTrackIndex` 저장·로드, `AdvancedOutput`, Multitrack Video에 전달하는 믹스 인덱스를 1~12번에 맞춘다. 주 12/VOD 11, 양쪽 12, VOD 미지원, 간단 출력 고정 배정에 대해 로컬 수신 또는 출력 구성 테스트로 기존 전송 수와 올바른 믹스를 확인한다.
- [x] 5.2 SRT/RIST의 후보 12개와 동시 최대 6개 제한을 분리해 UI·설정 저장·두 스트리밍 시작 경로에 적용한다. 1·2·3·7·11·12번 선택은 통과하고, UI 및 외부 수정 프로필의 7개 선택은 설명과 함께 거절되며 조용한 일부 누락이 없는지 확인한다.
- [x] 5.3 번들 obs-websocket의 트랙 조회·부분 변경·이벤트가 12개 상태를 다루는지 검증하고 필요한 코드·문서·테스트를 갱신한다. 12번 부분 변경, 기존 1~6번 키만 보낸 요청, 새 키의 비-boolean 값 거절, UI와 이벤트 상태 일치를 확인한다.

## 6. FFmpeg와 네이티브 출력 경로

- [x] 6.1 `obs-ffmpeg-mux.*`, `ffmpeg-mux` 보조 프로그램, MPEG-TS의 인코더 열거에서 배열 상한·헤더·패킷 인덱스·DTS 오프셋을 맞춘다. 일반 AAC/MKV 12개 및 희소 선택 녹화 파일을 `ffprobe`와 트랙별 디코딩으로 검사해 신호·순서·이름이 맞는지 확인한다.
- [x] 6.2 `obs-ffmpeg-output.*`의 선택 믹스 수, 믹스→스트림 순서, 트랙별 샘플 버퍼를 검증하고 필요한 수정을 한다. 사용자 지정 AAC/MKV와 AAC/MP4의 12개·12번 단독 출력에 누락·잘못된 믹스 연결이 없는지 확인한다.
- [x] 6.3 `plugins/obs-outputs/mp4-mux.c` 및 `mp4-output.c`의 동적 트랙·패킷 처리를 검증하고 필요한 수정을 한다. Hybrid MP4와 Hybrid MOV 12개 녹화·정상 종료·디코딩 및 지원되는 파일 분할에서 전체 트랙 집합이 유지되는지 확인한다.
- [x] 6.4 리플레이 저장의 상위 트랙 버퍼·타임스탬프 경로를 검증하고 필요한 수정을 한다. 12개 리플레이 반복 저장과 버퍼 중지 후 7·12번만 선택한 재시작에서 정확한 스트림 집합과 영상 구간 일치를 확인한다.
- [x] 6.5 `media-remux.c`와 OBS 수동·자동 리먹스의 전체 스트림·메타데이터 보존을 검증하고 필요한 수정을 한다. 12트랙 AAC/MKV→MP4에서 각 트랙의 순서·표현 가능한 이름·오디오 내용·시간 관계가 보존되고 재인코딩하지 않는지 확인한다.

## 7. 통합 및 플랫폼 검증

- [x] 7.1 설계서의 공통 출력 매트릭스를 실행한다. 간단 다중 녹화, 일반/Fragmented/Hybrid MP4, Hybrid MOV, MKV, 사용자 지정 FFmpeg, Opus/MKV, FLV 12번 단독의 파일을 검사하고 모든 트랙 디코딩·신호 분리 결과를 기록한다.
- [x] 7.2 지원되는 일시정지·재개와 분할 녹화, 방송 주 12/VOD 11 + 12트랙 녹화 + 리플레이 동시 실행을 검사한다. 시작·중지 순서를 바꿔도 오디오 집합·시간 관계·기존 방송 전송 수가 유지되는지 확인한다.
- [x] 7.3 같은 장면의 1/6/12트랙 CPU·메모리·파일 크기를 측정하고 12트랙 동기 시험을 30분 이상 실행한다. 초반·중반·후반에 모든 트랙을 디코딩해 설계의 시간 허용 범위와 6트랙 기준 대비 추가 누적 드리프트·오디오 누락 없음을 확인한다.
- [ ] 7.4 기존 Windows 빌드 구성으로 libobs·프런트엔드·번들 모듈을 빌드한다. Windows에서 12트랙 MKV 녹화·리플레이·MP4 리먹스·상위 방송/VOD 선택·100%/200% UI 검증을 실행하고 로그와 검사 결과를 기록한다.
- [ ] 7.5 기존 macOS 빌드 구성으로 libobs·프런트엔드·번들 모듈을 빌드한다. macOS에서 동일한 필수 녹화·리플레이·리먹스·방송 선택·배율 UI 검증을 실행하고 로그와 검사 결과를 기록한다.
- [x] 7.6 기존 Linux 빌드 구성으로 libobs·프런트엔드·번들 모듈을 빌드한다. Linux에서 동일한 필수 녹화·리플레이·리먹스·방송 선택·배율 UI 검증을 실행하고 로그와 검사 결과를 기록한다.

## 8. 사용 및 개발 문서

- [x] 8.1 12트랙 설정 방법, 새 소스와 기존 장면의 기본 배정 차이, 방송 선택과 동시 수 제한, 기존 단일 트랙 모드, 추가 플러그인 호환 제외, 구 OBS 롤백 방법을 문서화한다. 실제 UI·출력 동작과 일치하고 테스트 장면·명령·플랫폼별 결과로 요구사항을 추적할 수 있는지 확인한다.
- [x] 8.2 트랙 관련 잔여 6개 고정 코드·두 자리 이름 버퍼·매직 마스크를 범위 내에서 최종 점검한다. 의도적으로 남은 6이 구버전 이전 또는 방송 동시 수 제한임을 확인하고, 관련 포맷 검사·테스트 및 `openspec validate expand-audio-tracks-to-twelve --type change --strict --no-interactive`가 통과한 뒤 완료 항목을 갱신한다.

## 실행 결과

2026-09-15: 구현 및 Linux 검증 29/31 완료. 결과는 `docs/audio-tracks/validation-results.md`와 `linux-validation-summary.json`에 기록했다. Windows 7.4는 사용자 요청에 따라 `docs/audio-tracks/windows-validation-prompt.md`를 통한 별도 Windows 세션 실행으로 인계한다. macOS 7.5는 실행 환경이 없어 미검증으로 유지한다.
